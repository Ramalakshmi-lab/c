#include<stdio.h>
int main()
{
    int s;
    scanf("%d",&s);
    int c=s*s,n=0,r;
    while(c>0)
    {
        r=c%10;
        n=n+r;
        c=c/10;
    }
    if(s==n)
    {
        printf("Neon Number");
    }
    else{
        printf("Not Neon Number");
    }
}
